name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      - uses: dtolnay/rust-toolchain@stable

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri app
        run: npm run tauri build

      - name: Ad-hoc sign the app
        run: |
          codesign --force --deep --sign - \
            "src-tauri/target/release/bundle/macos/notch-now.app"

      - name: Create DMG
        run: |
          mkdir -p /tmp/notch-now-pkg
          cp -r "src-tauri/target/release/bundle/macos/notch-now.app" /tmp/notch-now-pkg/
          hdiutil create \
            -volname "notch-now" \
            -srcfolder /tmp/notch-now-pkg \
            -ov -format UDZO \
            "notch-now-${{ github.ref_name }}-macos.dmg"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "doing-now ${{ github.ref_name }}"
          files: notch-now-${{ github.ref_name }}-macos.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
